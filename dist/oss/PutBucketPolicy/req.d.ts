export interface PutBucketPolicyRequest {
    /**
     * Bucket名称。
     * @example `examplebucket`
     */
    "bucket": string;
    /**
     * 请求体。
     * @example `二进制内容`
     */
    "body": string;
}
