export interface QueryRecordUrlByTimeResponse {
    /**
     * 接口返回码：
     * - **200**：表示成功。
     * - 其它：表示错误码。错误码详情，请参见[错误码](~~145071~~)。
     * @example `200`
     */
    Code: string;
    /**
     * 录像文件的URL。URL包含鉴权信息，且无法重复使用。
     * 录像文件的URL对应指定时间范围内所有的录像文件，将所有录像文件合并播放。
     * @example `https://linkvisual.aliyun.com/ddsffa****.m3u8`
     */
    Data: string;
    /**
     * 调用失败时，返回的出错信息。
     * @example `tenant auth error`
     */
    ErrorMessage: string;
    /**
     * 阿里云为该请求生成的唯一标识符。
     * @example `06DC77A0-4622-42DB-9EE0-25FIOHS82JK1`
     */
    RequestId: string;
    /**
     * 是否调用成功：
     * - **true**：调用成功。
     * - **false**：调用失败。
     * @example `true`
     */
    Success: boolean;
}
