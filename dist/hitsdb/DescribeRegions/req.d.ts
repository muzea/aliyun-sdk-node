export interface DescribeRegionsRequest {
    /**
     * 返回结果中地域名称（LocalName）的显示语言，取值：
     * - **zh-CN**：中文，默认值。
     * - **en-US**：英文。
     * @example `zh-CN`
     */
    "AcceptLanguage"?: string;
}
