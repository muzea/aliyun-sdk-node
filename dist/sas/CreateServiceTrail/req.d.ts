export interface CreateServiceTrailRequest {
    /**
     * 要查询的实例所在地域的ID。取值：
     * - **cn-hangzhou**：中心
     * - **ap-southeast-1**：新加坡
     * @example `cn-hangzhou`
     */
    "RegionId"?: string;
}
