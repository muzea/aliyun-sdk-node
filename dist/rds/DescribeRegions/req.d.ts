export interface DescribeRegionsRequest {
    /**
     * 指定返回的**LocalName**参数的语言。取值：
     * * **zh-CN**：中文
     * * **en-US**：英文
     * 默认值：**en-US**
     * @example `en-US`
     */
    "AcceptLanguage"?: string;
}
