export interface DescribeRegionsRequest {
    /**
     * 指定返回参数**LocalName**对应的值的语言，取值：
     * * **zh-CN**：中文。
     * * **en-US**：英文。
     * > 默认值：**zh-CN**。
     * @example `zh-CN`
     */
    "AcceptLanguage"?: string;
}
